---
"fiber": minor
---

Response bodies stream into the pane as they arrive, so a slow endpoint shows its answer while it is still being written and an SSE stream shows anything at all. Importing an OpenAPI spec now fills in a request body from the operation's schema. Collections have a settings cog in the sidebar. Loader templates lead with OpenAPI, and a new loader starts on it. Option-arrow in a URL field jumps between dots and slashes, the way a browser's address bar does.
