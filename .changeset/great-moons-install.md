---
"fiber": patch
---

Add a macOS install script that sidesteps the Gatekeeper prompt entirely, by fetching the release with curl — which, unlike a browser, attaches no quarantine attribute for macOS to ask about.
