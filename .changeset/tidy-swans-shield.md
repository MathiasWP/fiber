---
"fiber": patch
---

Text can go down to 7px. Section settings keeps one height across its tabs instead of resizing under you, and a collection with auth shows whether a credential is actually stored.
