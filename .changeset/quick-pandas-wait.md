---
"fiber": minor
---

The waiting state reads better: a white loader, larger label, and messages that change as a slow request drags on. Updates can now be deferred to the next launch instead of restarting immediately, and the window comes back where you left it — in front — after an update restarts the app.
