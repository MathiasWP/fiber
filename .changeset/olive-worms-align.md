---
"fiber": patch
---

Three bits of sidebar and response-pane polish: a much larger loader while a request is in flight, matched optical heights for the new-request and new-collection icons, and a base URL chip that sizes to its own text instead of truncating at a fixed width.
