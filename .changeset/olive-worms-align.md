---
"fiber": patch
---

Sidebar and response-pane polish: a much larger loader while a request is in flight, matched optical heights for the new-request and new-collection icons, a base URL chip that sizes to its own text, request names shown and searchable in the History list, and the redundant response-pane history strip removed.
