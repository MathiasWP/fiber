---
"fiber": minor
---

Query params replace the dead body tab on GET and HEAD, editing the URL directly. Right-clicking the collection list offers New collection and New request, and the webview's own "Reload" menu no longer appears where the app has nothing to offer.
