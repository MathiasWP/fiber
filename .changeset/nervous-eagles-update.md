---
"fiber": minor
---

Update in place. The toast now downloads the new version with a progress bar, verifies its signature, installs it and restarts — instead of sending you to a browser to find a .dmg.
