---
"fiber": minor
---

Section settings is a drawer sliding out from the sidebar's edge rather than a centred dialog that resized on every tab, and CodeMirror's find panel is styled to match the app instead of showing raw browser controls.
