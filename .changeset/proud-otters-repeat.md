---
"fiber": patch
---

Loaders pointed at an OpenAPI document now fill in request bodies from each operation's schema, the way importing the same file already did. The templates dropdown names the template in use and ticks it in the list, and Done runs the loader rather than leaving you to find Refresh.
