---
"fiber": minor
---

Headers can be removed with an X, and the collection's auth header is shown in the table where it lands. Its value stays write-only — pasting a new token replaces it for the whole collection.
